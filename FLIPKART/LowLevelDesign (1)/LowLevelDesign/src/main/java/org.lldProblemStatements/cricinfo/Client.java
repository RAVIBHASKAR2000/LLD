package main.java.org.lldProblemStatements.cricinfo;

public class Client {
    public static void main(String[] args){
        /*
            Class CricInfoApp:
            + List<Match> getAllMatches() // return all the upcoming matches
            + List<Team> getAllTeams(Event event) // return all the teams
            + List<Match> getAllLiveMatches() // fetch all the matches that is running right now
            + List<Match> getAllCompletedMatches() // return all the matches that are completed
            + List<Match> searchMatch(Team team) //return all the matches in which a team is involved
            + Player searchPlayer(String playerName)
            + Player searchTeam(String teamName)
         */
        /*
            Class Events:
            - List<Matches> matches
            - List<Teams> teamsPlaying
         */
        /*
            Class Team
            - List<Players> players
         */
        /*
            Class Match
            - ScoreCard scoreCard
            - Commentary commentary
            - Stats stats
         */
        /*
            Class Player
         */
        /*
            Class ScoreCard
         */
        /*
            Class Commentary
         */
        /*
            Class Statistics
         */
    }
}
